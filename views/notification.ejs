<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/src/css/user_dashboard.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <section class="notification">
        <div class="wrap">
            <% if(data.notification){%>

                <% data.notification.forEach(notification=>{%>
                    <div class="person">
                        <a href="/user/notification/view?id=<%= notification.id %> ">
                            <div class="icon">
                                <img src="https://img.icons8.com/doodle/48/000000/user-male--v1.png" />
                            </div>
                            <p><%= notification.username %> </p>
                        </a>
                    </div>
                <%}) %>  
           <% }else{%>
                    <h4 style="color: orange; background-color: white;
                    text-align: center; margin-bottom: 10px; border-top: 10px solid black;
                    padding: 10px;">You currently do not have any notification</h4>
          <% } %> 
        </div>
    </section>
</body>
</html>